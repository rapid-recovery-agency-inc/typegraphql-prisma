# Required for integration tests (test 4: integration.ts).
# This must point to a running PostgreSQL instance (not the system one used by Prisma).
# Example: postgresql://postgres:password@localhost:5432/typegraphql_prisma_test
TEST_DATABASE_URL=
